# Hand_Detection_gestures
# Problem Statement


* Create a Volume Control using hand landmark detection and sign detection using Mediapipe framework and OpenCV. The project will use Google’s Hand Landmark model.

